<% provide(:title, 'Blog') %>

  <%= render 'shared/navbar' %>
  <div class="container-fluid userContain">

    <%= render 'shared/flash' %>

    <div class="row">
      <aside class="col-md-4">
        <section class="user_info">
          <%= render 'shared/user_info' %>
        </section>

        <section class="micropost_form">
          <%= render 'shared/micropost_form' %>
        </section>
      </aside>

      <div class="col-md-8">
        <div class="divHeader">
          <h1>Microposts Feed</h1>
        </div>
        <%= render 'shared/feed' %>
      </div>
    </div>

  </div>
