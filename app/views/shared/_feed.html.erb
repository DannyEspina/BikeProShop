<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
  <% else %>
      <br>
     <%= image_tag('Users/noPost.png', alt:"no post", class:"noPost") %>
<% end %>